let lastName = "Doe";
let firstName = "John";
let city = "New-York";

function simpleCap(str) {
  return str[0].toUpperCase() + str.slice(1);
}

alert(`Nom : ${lastName}\nPrénom : ${firstName}\nville : ${city}\n`);
